# Tokens

The canonical brand color is `--color-brand: #7654FF`.

```css
:root {
  --color-brand: #7654FF;
}
```

Implementation reference: [Token adapter](https://example.invalid/acme/token-adapter)
